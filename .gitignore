# This is cache of the CSS generated by the LESS parser.
#app/public/css/style.css

# Node is compiled on the machine during deployment.
lib/node

# The Node source is altered during the build. We let the initialize_node
# command re-download it to ensure it compiles properly for each machine.
src/node

# This ignores Compound's cached complied .css and .js files.  It should be
# fine to commit the cached files connectAssets makes.
#public/stylesheets/cache_*
#public/javascripts/cache_*
#app/public/stylesheets/cache_*
#app/public/javascripts/cache_*
